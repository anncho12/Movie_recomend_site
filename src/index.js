import React from 'react';
import ReactDOM from 'react-dom/client';//원래는 'react-dom/clint'
import App from './App';
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <App />
);